name: center_spot
description: A minimal, frame-free Flutter viewport preview centered on a preset size.
version: 0.1.0
repository: https://github.com/muhammadsaddamnur/center_spot
topics:
  - preview
  - responsive
  - development

environment:
  sdk: ^3.11.4
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: false
